version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "installing dependencies"
      - npm install 
   build:
     commands:
       - echo "building the application"
       - npm run build
artifacts:
  files:
    - dist/**/*
  discard-paths: no
  
